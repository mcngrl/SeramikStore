@* <auto-generated>
This file is generated. Do not edit manually.
Generated at      : 2026-01-25 01:45:27
Generated at (UTC): 2026-01-24 22:45:27
Machine: DESKTOP-IITN2G2
Generator: EntityCreator v1.0
Object Name: ViewWriter
</auto-generated> *@

@using Microsoft.Extensions.Localization
@using SeramikStore.Web.Localization
@model IEnumerable<SeramikStore.Web.ViewModels.Potansiyel.PotansiyelListViewModel>
@inject IStringLocalizer<PotansiyelResource> L
@inject IStringLocalizer<CommonResource> C

<h2 class="mb-3">@L["ListTitle"]</h2>
<a asp-action="Create"
   class="btn btn-sm btn-outline-success rounded-pill px-4 mb-3">
    <i class="fa fa-file me-1"></i>@C["New"]
</a>
<table class="table table-bordered table-hover align-middle">
    <thead class="table-light">
        <tr>
            <th>@L["Name"]</th>
            <th>@L["SurName"]</th>
            <th>@L["KnownName"]</th>
            <th>@L["Email"]</th>
            <th>@L["PhoneNumber"]</th>
            <th>@L["Adress"]</th>
            <th>@L["tarih"]</th>
            <th style="width:200px"></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr class="clickable-row" data-edit-url="@Url.Action("Edit", "Potansiyel", new { id = item.Id })">
            <td>@item.Name</td>
            <td>@item.SurName</td>
            <td>@item.KnownName</td>
            <td>@item.Email</td>
            <td>@item.PhoneNumber</td>
            <td>@item.Adress</td>
            <td>@item.tarih</td>
            <td class="text-nowrap action-cell">
                <a class="btn btn-sm btn-outline-warning rounded-pill px-3 me-1"
                   asp-action="Edit"
                   asp-route-id="@item.Id">
                    <i class="fa fa-edit me-1"></i>@L["Edit"]
                </a>
                <a class="btn btn-sm btn-outline-primary rounded-pill px-3 me-1"
                   asp-action="Detail"
                   asp-route-id="@item.Id">
                    <i class="fa fa-eye me-1"></i>@L["Detail"]
                </a>
                <a class="btn btn-sm btn-outline-danger rounded-pill px-3"
                   asp-action="Delete"
                   asp-route-id="@item.Id">
                    <i class="fa fa-trash me-1"></i>@L["Delete"]
                </a>
            </td>
        </tr>
    }
    </tbody>
</table>
@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {

            document.querySelectorAll(".clickable-row").forEach(row => {
                row.addEventListener("click", function () {
                    const url = this.dataset.editUrl;
                    if (url) {
                        window.location.href = url;
                    }
                });
            });

            document.querySelectorAll(".action-cell").forEach(cell => {
                cell.addEventListener("click", function (e) {
                    e.stopPropagation();
                });
            });
        });
    </script>
}
<style>
    .clickable-row {
        cursor: pointer;
    }
    .clickable-row:hover {
        background-color: #f8f9fa;
    }
</style>
