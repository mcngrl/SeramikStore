@using SeramikStore.Web.ViewModel
@model LoginViewModel
@{
    ViewData["Title"] = "Giriş Yap";
}


<form asp-action="Login" method="post">

    <div class="container mt-5 d-flex align-items-center justify-content-center">
        <div class="row w-100 justify-content-center">
            <div class="col-md-5 col-sm-8">
                <div class="card shadow-lg border-0">
                    <div class="card-header text-center bg-primary text-white">
                        <h4 class="mb-0">🔐 Üye Girişi</h4>
                    </div>
     

                    <div class="card-body p-4">
                        <div class="form-group mb-3">
                            <label>Email</label>
                            <input asp-for="UserName" class="form-control" placeholder="Email adresiniz" />
                            <span asp-validation-for="UserName" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label>Şifre</label>
                            <input asp-for="Password" type="password" class="form-control" placeholder="Şifreniz" />
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>

                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div class="alert alert-danger">
                                @Html.ValidationSummary(false)
                            </div>
                        }

                        <button type="submit" class="btn btn-primary w-100">
                            <i class="fas fa-sign-in-alt"></i> Giriş Yap
                        </button>
                    </div>

                    <div class="card-footer text-center">
                        <small>
                            Hesabın yok mu?
                            <a asp-action="Register" asp-controller="Account">Kayıt Ol</a>
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

<form asp-action="Login" method="post">
    <input asp-for="UserName" class="form-control" value="admin@admin.com" placeholder="Email adresiniz" />
    <input asp-for="Password" type="password" class="form-control" value="321321" placeholder="Şifreniz" />
    <button type="submit">
        <i class="fas fa-sign-in-alt"></i> Admin Olarak Gir
    </button>
</form>
<form asp-action="Login" method="post">
 
        <input asp-for="UserName" class="form-control" value="customer@customer.com" placeholder="Email adresiniz" />
        <input asp-for="Password" type="password" class="form-control" value="321321" placeholder="Şifreniz" />
    <button type="submit">
        <i class="fas fa-sign-in-alt"></i> Müşteri Olarak Gir
    </button>
</form>



@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}