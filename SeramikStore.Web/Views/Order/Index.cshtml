@model List<SeramikStore.Services.DTOs.OrderListItemDto>

@{
    ViewData["Title"] = "Siparişlerim";
}

<div class="container mt-4">
    <h3 class="mb-4">Siparişlerim</h3>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            Henüz bir siparişiniz bulunmamaktadır.
        </div>
    }
    else
    {
        <table class="table table-bordered table-hover align-middle">
            <thead class="table-light">
                <tr>
                    <th>#</th>
                    <th>Tarih</th>
                    <th>Durum</th>
                    <th>Ödeme</th>
                    <th class="text-end">Toplam</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.OrderId</td>
                        <td>@item.OrderDate.ToString("dd.MM.yyyy HH:mm")</td>
                        <td>
                            <span class="badge
                                        @(item.OrderStatus == "PaymentWaiting" ? "bg-warning" :
                                                                    item.OrderStatus == "Paid" ? "bg-success" :
                                                                    "bg-danger")">
                        @item.OrderStatus
                    </span>
                </td>
                <td>@item.PaymentType</td>
                <td class="text-end fw-bold">
                    @item.GrandTotal.ToString("N2") ₺
                </td>
                <td class="text-end">
                    <a asp-action="OrderInfo"
                       asp-route-id="@item.OrderId"
                       class="btn btn-sm btn-outline-primary">
                        Detay
                    </a>
                </td>
            </tr>
                        }
            </tbody>
        </table>
    }
</div>
